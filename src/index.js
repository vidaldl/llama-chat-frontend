import React from 'react';
import ReactDOM from 'react-dom';
import './style.css'; // Make sure this matches your current CSS filename and path
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
